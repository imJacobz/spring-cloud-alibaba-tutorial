spring:
  application:
    name: nx-auto-ms-auth
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        group: DEFAULT_GROUP
        prefix: nx-auto-default-config